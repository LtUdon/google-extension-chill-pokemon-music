# Notes
## Core components a Chrome Extension Popup
- `manafest.json` allows the browser to load the extention.
    - The `"action"` key declares the image Chrome should use as the extension's action icon and the HTML page to show in a popup when the extension's action icon is clicked.